server {
    listen   3000 default;

    root /var/www/mysite;
    index index.html index.htm;

    sendfile off;

    error_page   500 502 503 504  /50x.html;

    location / {
        try_files $uri /index.html;
    }
}